{
    "version": 2,
    "builds": [
      {
        "src": "index.js",
        "use": "@vercel/node"
      }
    ],
    "installCommand": "npm install",
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
  }